#include "main.h"

/**
 * print_square - prints a square
 * @size: of size
 *
 * Return: nothing
 */

void print_square(int size)
{
int i = j = 0;
if (size <= 0)
{
_putchar(10);
}
else
{
while (i < size)
{
while (j < size)
{
_putchar(35);
j++;		}
_putcha(10);
i++;
}
}
}
