#include "main.h"

/**
 * _abs - find absolute value of a number
 * @num: the number
 *
 * Return: the number as absolute value
 */

int _abs(int num)
{
if (num > 0)
	return (num);
else
	return (-num);
}
