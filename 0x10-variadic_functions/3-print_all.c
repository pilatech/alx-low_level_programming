#include <stdio.h>
/**
 * print_all - prints anything based on format.
 * @format: types of arguments.
 *
 * Return: Nothing
 */
void print_all(const char * const format, ...)
{
	/* create the base for the loop by looping over the format */
		/* for each character ==> if it is among the the allowed format, push to a new string */
	/* set arg_list with the the va_list and format */
	/* loop until count the new string is not null terminated */
	/* check if the current format:w
}
